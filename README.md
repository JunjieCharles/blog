# 俊杰Charles

一个无需构建步骤的静态博客，可直接部署到 GitHub Pages。

## 本地预览

在仓库目录运行任意静态文件服务器，例如：

```powershell
python -m http.server 8000
```

然后访问 `http://localhost:8000`。

## GitHub Pages

将仓库推送到 GitHub 后，在仓库的 **Settings → Pages** 中选择从当前分支的根目录发布即可。
